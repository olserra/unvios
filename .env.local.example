# Example environment variables for unvios-app
# Copy this to .env.local and edit values for local development.

# Authentication
AUTH_SECRET=super-secret-dev-key

# LLM configuration
# For OpenAI (requires an API key):
# LLM_API_URL=https://api.openai.com/v1/chat/completions
# LLM_API_KEY=sk-...
# LLM_MODEL=gpt-4o

# For Ollama (local/no-key by default):
# LLM_API_URL=http://localhost:11434
# No LLM_API_KEY required for typical Ollama local usage.

# Development helpers
# When set to 1, the server will respond with a deterministic mock reply
# for authenticated users so you can develop the UI without an LLM.
LLM_DEV_MOCK=1

# Optional: override default model for OpenAI
LLM_MODEL=gpt-3.5-turbo
